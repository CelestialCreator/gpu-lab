---
# SDXL LoRA Training — v3 (10000 steps)
# Status: SUCCESS — final production run
#
# Full training run with all optimizations applied from previous iterations.
# 10000 steps with the complete 17-image dataset produced the best results
# with strong likeness and prompt adherence.
#
job: extension
config:
  name: "my_character_v3"
  process:
    - type: 'sd_trainer'
      training_folder: "/workspace/output"
      device: cuda:0
      trigger_word: "TRIGGER_WORD"

      network:
        type: "lora"
        linear: 32
        linear_alpha: 32

      save:
        dtype: float16
        save_every: 1000
        max_step_saves_to_keep: 4
        push_to_hub: false

      datasets:
        - folder_path: "/workspace/datasets/my_character"
          caption_ext: "txt"
          caption_dropout_rate: 0.05
          shuffle_tokens: false
          cache_latents_to_disk: true
          resolution:
            - 512
            - 768
            - 1024

      train:
        batch_size: 1
        steps: 10000
        gradient_accumulation_steps: 2
        train_unet: true
        train_text_encoder: false
        gradient_checkpointing: true
        noise_scheduler: "ddpm"
        optimizer: "adamw8bit"
        lr: 5e-5
        dtype: bf16

      model:
        name_or_path: "stabilityai/stable-diffusion-xl-base-1.0"
        arch: "sdxl"
        quantize: false
        low_vram: false

      sample:
        sampler: "ddpm"
        sample_every: 1000
        width: 1024
        height: 1024
        prompts:
          - "a photo of TRIGGER_WORD smiling at a cafe, natural lighting"
          - "TRIGGER_WORD in a professional headshot, studio lighting, white background"
          - "a portrait of TRIGGER_WORD outdoors, golden hour, bokeh background"
        neg: "blurry, low quality, deformed"
        seed: 42
        walk_seed: true
        guidance_scale: 7.5
        sample_steps: 25
